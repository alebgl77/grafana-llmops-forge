global:
  scrape_interval: 10s
  evaluation_interval: 15s
rule_files:
  - /etc/prometheus/rules/*.yml     # y déposer prometheus_rules_llmops.yml
scrape_configs:
  - job_name: llm-emitter
    static_configs: [{targets: ["emitter:9109"]}]
